name: "Build Image Robot"

on:
  workflow_dispatch:
  
env:
  CONTAINER_REGISTRY: pramotser/robot-framework
  TAG_IMAGE: "0.0.1"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Docker Login"
        uses: docker/login-action@v1.10.0
        with: 
          registry: docker.io
          username: pramotser
          password: Pram0tser
      
